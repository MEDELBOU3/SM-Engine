(function () {
    const root = window.SMModifiers = window.SMModifiers || {};
    root.StackOptimizer = class StackOptimizer {
        optimize(stack) {
            return stack;
        }
    };
})();
